package com.assignment.task.util;

import javax.persistence.AttributeOverride;

public enum KeyWords {

    SUCCESSFUL,
    CONFLICT,
    EXIST,
    NON_EXIST,
    UNSUCCESSFUL,
    INVALID_CREDENTIALS,
}
